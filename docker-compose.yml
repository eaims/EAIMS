services:
  validator:
    build:
      context: .
      dockerfile: Dockerfile.validator
    image: eaims/validator:1.1.0
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
